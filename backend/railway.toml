[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter backend run build"

[deploy]
startCommand = "pnpm --filter backend run start:prod"
releaseCommand = "pnpm --filter backend run db:migrate:deploy"
restartPolicyType = "on_failure"
